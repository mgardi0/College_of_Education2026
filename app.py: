import streamlit as st

st.set_page_config(page_title="کۆلێژی پەروەردە", page_icon="🎓")
st.title("زانکۆی سەلاحەدین - کۆلێژی پەروەردە")
st.write("بەخێربێیت بۆ سیستەمی تاقیکردنەوە")

choice = st.radio("پرسیار: ئایا پایتۆن زمانی پڕۆگرامسازییە؟", ["بەڵێ", "نەخێر"])
if st.button("پشکنین"):
    if choice == "بەڵێ":
        st.success("ڕاستە!")
        st.balloons()
    else:
        st.error("هەڵەیە!")
